# Streamlit Cloud (Python 3.14) currently resolves streamlit 1.61 + starlette 1.4,
# which crashes at runtime:
#   TypeError: GZipResponder.__init__() missing ... 'thread_minimum_size'
# https://github.com/streamlit/streamlit/issues/16341
#
# Pin starlette to 1.3.1 (compatible constructor). Do NOT pin old pandas/numpy —
# those lack cp314 wheels and break the Cloud install.
streamlit==1.61.0
starlette==1.3.1
pandas
openpyxl
